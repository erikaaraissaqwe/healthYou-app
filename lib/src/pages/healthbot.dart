import 'package:flutter/material.dart';

// ignore: must_be_immutable
class HealthBotPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text('health bot');
  }
}
